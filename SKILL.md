---
name: github-publisher
description: 当用户想把 Skill 或 Agent 发布到 GitHub 时自动触发。读取目标 Skill 的代码和注释，生成高星开源项目风格的完整 README 文档，然后自动执行 git add、commit、push 完成发布。使用时机：用户说"发布"、"push 到 GitHub"、"上传技能"、"publish"、"发布我的 Skill" 时必须调用此 Skill。
---

# GitHub Publisher Skill

## 职责

读取任意 Skill 或 Agent 的源文件，生成专业的 GitHub 文档，并完成自动发布。

## 执行流程

### Phase 1：信息收集

1. 确认目标 Skill 的路径（让用户指定，或扫描当前目录）
2. 读取以下文件：
   - `SKILL.md` 或主逻辑文件
   - 所有 `references/` 下的文档
   - 所有 `scripts/` 下的代码
   - 已有的 `README.md`（如果存在）
3. 提取关键信息：
   - 技能名称和核心功能
   - 输入/输出格式
   - 依赖项和兼容性
   - 使用场景和触发条件

### Phase 2：生成 README

参考 `references/readme-template.md` 的结构，生成以下完整内容：

**必须包含的章节（按顺序）：**

1. **项目标题 + 徽章行**（Stars / License / Version）
2. **一句话介绍**（精准描述核心价值）
3. **功能演示 GIF 或截图占位**（提示用户补充）
4. **核心功能**（3-5 个要点，每条有具体说明）
5. **快速开始**（最短路径让用户跑起来）
6. **详细使用说明**（覆盖所有参数和选项）
7. **配置项**（表格形式，含默认值和说明）
8. **使用示例**（至少 3 个真实场景）
9. **项目结构**（目录树 + 各文件说明）
10. **贡献指南**
11. **License**

**写作规范（参考 `references/style-guide.md`）：**

- 标题用 emoji 开头，增加视觉层次
- 代码示例必须可以直接运行
- 避免废话，每句话都有信息量
- 使用表格展示配置项，比列表更清晰
- 英文为主，但可以中英双语

### Phase 3：质量检查

生成后自检以下项目：

- [ ] 所有代码块都有语言标注
- [ ] 安装步骤可以一步步跟着做
- [ ] 没有"TODO"或空白占位符（除演示媒体外）
- [ ] 配置表格的每一行都有默认值

### Phase 4：Git 发布

```bash
# 进入 Skill 目录
cd <skill-path>
# 写入 README
# （已由 Phase 2 完成）
# 提交并推送
git add README.md
git commit -m "docs: auto-generate README for <skill-name>
- Generated by github-publisher skill
- Covers: features, usage, config, examples, structure"
git push origin main
```

执行后告知用户：发布成功的仓库链接。

## 注意事项

- 如果 Skill 已有 README，先对比差异，只更新缺失部分
- 如果 git push 失败，告知用户检查 remote 配置
- 生成的文档语气要专业但不生硬，像真人写的一样
