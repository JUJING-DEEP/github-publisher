---
name: github-publisher
description: 当用户想把 Skill 或 Agent 发布到 GitHub 时自动触发。读取目标 Skill 的代码和注释，生成高星开源项目风格的完整 README 文档，然后自动执行 git add、commit、push 完成发布。使用时机：用户说"发布"、"push 到 GitHub"、"上传技能"、"publish"、"发布我的 Skill" 时必须调用此 Skill。
---

# GitHub Publisher Skill

## 职责

读取任意 Skill 或 Agent 的源文件，生成专业的 GitHub 文档，并完成自动发布。

---

## 触发条件

用户说以下任意内容时自动触发：
- 「发布」「push 到 GitHub」「上传技能」
- 「publish」「发布我的 Skill」
- 「帮我生成 README」「生成文档」

---

## 执行流程

### Phase 1：信息收集

1. **确认目标 Skill 的路径**
   - 用户指定路径 → 使用该路径
   - 用户未指定 → 扫描当前目录的 `skills/` 子目录，让用户选择
2. **读取以下文件：**
   - `SKILL.md` 或主逻辑文件
   - 所有 `references/` 下的文档
   - 所有 `scripts/` 下的代码
   - 已有的 `README.md`（如果存在）
3. **提取关键信息：**
   - 技能名称和核心功能
   - 输入/输出格式
   - 依赖项和兼容性
   - 使用场景和触发条件

### Phase 2：生成 README

参考 `references/readme-template.md` 的结构，生成以下完整内容：

**必须包含的章节（按顺序）：**

1. **项目标题 + 徽章行**（Stars / License / Version）
2. **一句话介绍**（精准描述核心价值）
3. **功能演示 GIF 或截图占位**（提示用户补充）
4. **核心功能**（3-5 个要点，每条有具体说明）
5. **快速开始**（最短路径让用户跑起来）
6. **详细使用说明**（覆盖所有参数和选项）
7. **配置项**（表格形式，含默认值和说明）
8. **使用示例**（至少 3 个真实场景）
9. **项目结构**（目录树 + 各文件说明）
10. **贡献指南**
11. **License**

**写作规范（参考 `references/style-guide.md`）：**

- 标题用 emoji 开头，增加视觉层次
- 代码示例必须可以直接运行
- 避免废话，每句话都有信息量
- 使用表格展示配置项，比列表更清晰
- 英文为主，但可以中英双语

### Phase 3：质量检查

生成后自检以下项目：

- [ ] 所有代码块都有语言标注
- [ ] 安装步骤可以一步步跟着做
- [ ] 没有"TODO"或空白占位符（除演示媒体外）
- [ ] 配置表格的每一行都有默认值

### 🔴 CHECKPOINT · 用户确认

展示生成的 README 预览，询问用户是否满意：
- **用户说 yes/确认** → 继续 Phase 4
- **用户说 no/不满意** → 询问具体问题，针对性修改后重新展示

### Phase 4：Git 发布

```bash
# 进入 Skill 目录
cd <skill-path>
# 写入 README（已由 Phase 2 完成）
# 提交并推送
git add README.md
git commit -m "docs: auto-generate README for <skill-name>
- Generated by github-publisher skill
- Covers: features, usage, config, examples, structure"
git push origin main
```

执行后告知用户：发布成功的仓库链接。

---

## 失败模式与 Fallback（Dim 3）

### 场景 1：Git 仓库不存在

| 触发条件 | 一线修复 | 仍失败则 |
|---------|---------|---------|
| `git init` 失败或目录不在 git 仓库 | 询问用户：「需要我帮你初始化 git 仓库吗？」 | 用户拒绝 → 在当前目录生成 README.md 文件，不执行 git 操作 |
| remote 未配置 | 提示用户配置 remote 或使用 Token URL | 用户无法配置 → 告知用户手动执行 git push |

### 场景 2：README 已存在

| 触发条件 | 一线修复 | 仍失败则 |
|---------|---------|---------|
| 目标目录已有 README.md | 对比新旧内容差异 | 差异过大 → 询问用户「覆盖」还是「只更新缺失部分」 |
| 用户不想覆盖 | 保留原 README，只更新缺失章节 | 用户坚持覆盖 → 备份后覆盖 |

### 场景 3：Git Push 失败

| 触发条件 | 一线修复 | 仍失败则 |
|---------|---------|---------|
| `git push` 返回错误（非权限或网络问题） | 检查 remote URL 是否正确，尝试重新认证 | Token 无效 → 提示用户检查/更新 Token |
| 网络超时 | 重试最多 3 次，每次间隔 5 秒 | 重试失败 → 告知用户「稍后手动执行 git push」 |

### 场景 4：目标 Skill 路径无效

| 触发条件 | 一线修复 | 仍失败则 |
|---------|---------|---------|
| 指定路径不存在或无 SKILL.md | 扫描当前目录 `skills/` 子目录，列出可用 Skill | 扫描为空 → 告知用户「未找到可用 Skill，请指定有效路径」 |

---

## 反例与黑名单（Dim 9）

**禁止做的事：**

1. ❌ **不要编造内容** — 只基于读取到的真实文件内容生成 README，不添加未提及的功能
2. ❌ **不要跳过质量检查** — Phase 3 的 4 项检查必须逐项确认后才能进入 Phase 4
3. ❌ **不要强制覆盖用户已有的 README** — 必须先询问用户意愿
4. ❌ **不要在无 git 仓库时执行 git push** — 先初始化或询问用户
5. ❌ **不要使用模糊措辞** — 避免「建议」「可以考虑」「根据情况」等软化词
6. ❌ **不要忽略错误信息** — 任何 git 命令失败都要告知用户并给出解决方案
7. ❌ **不要生成空占位符内容** — 代码示例必须可运行，配置项必须有具体默认值

**危险操作警告：**

- ⚠️ 执行 `git push --force` 前必须确认用户意图
- ⚠️ 删除已有文件前必须备份或确认用户意愿
- ⚠️ 覆盖 README 前必须先对比差异并获得用户确认

---

## 注意事项

- 如果 Skill 已有 README，先对比差异，只更新缺失部分
- 如果 git push 失败，告知用户检查 remote 配置
- 生成的文档语气要专业但不生硬，像真人写的一样
